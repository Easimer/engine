#pragma once

#include <cstdint>
#include <type_traits>

namespace gfx {
	// Model handle
	using model_id = uint32_t;
	// Shader handle
	using shader_id = int;
	// Material handle
	using material_id = size_t;
}
